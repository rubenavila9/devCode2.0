<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ariel.az.devcode20">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:requestLegacyExternalStorage="true">
        <activity android:name=".Activities.AdminActivity" android:label="@string/denunciasAdmin">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.Principal" />
        </activity>

        <activity android:name=".Activities.DetailsPublicationsActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.Principal" />
        </activity>
        <activity android:name=".Activities.IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activities.Principal" android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".Activities.LoginActivity" android:noHistory="true" android:label="@string/login"/>
        <activity android:name=".Editarperfildelusuario" />
        <activity android:name=".Activities.RegisterActivity" android:label="@string/register">
            <meta-data
                    android:name="android.support.PARENT_ACTIVITY"
                    android:value=".Activities.LoginActivity" />
        </activity>
    </application>

</manifest>